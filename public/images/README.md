# Image assets

Put website images in this folder. Files here are served publicly from `/images/...`.

Current hero image: `ambulance-hero-placeholder.svg`.

To replace it, add your image (for example `ambulance-hero.jpg`) and update the `src` value in `app/page.tsx`.
